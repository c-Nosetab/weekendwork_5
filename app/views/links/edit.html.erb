<h1>Edit link:</h1>

<%= form_tag "/links/#{@link.id}", method: :patch do %>

  <div>
    <%= label_tag :slug %>
    <%= text_field_tag :slug, @link.slug %>
  </div>
  <div>
    <%= label_tag :target_url %>
    <%= text_field_tag :target_url, @link.target_url%>
  </div>
  <%= submit_tag "Edit Link", class: "btn btn-primary"%>

  <!-- <div>
    <h2>Slug:  <input type="text" name="slug" value="<!%= @link.slug %>"></h2>
  </div>
  <div>
    <h2>Target URL:  <input type="text" name="target_url" value="<!%= @link.target_url %>"></h2>
  </div> -->


<% end %>