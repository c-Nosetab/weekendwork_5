
<div class="well content">
  <h1>Edit link:</h1>

<h2>
  <%= form_tag "/links/#{@link.id}", method: :patch do %>

    <div>
      <%= label_tag :slug %>
      <%= text_field_tag :slug, @link.slug %>
    </div>
    <div>
      <%= label_tag :target_url %>
      <%= text_field_tag :target_url, @link.target_url%>
    </div>
    <%= submit_tag "Edit Link", class: "btn btn-primary"%>



  <% end %>
  </h2>
</div>